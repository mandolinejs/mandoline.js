// `treeml`: template tag
// syntax to define graph as a set of overlapping trees (examples below)
// forking paths that each start at some root and, together, reach every node
export default function treeml(
  strings: Array<string>,
  ...keys: Array<unknown>,
) {
  //interpolate keys?
  builder.push(key);
  builder.add_edge(key);

  declare const input: string;

  const depth_map: Record<number, number | undefined>;

  for (const line of lines) {

  }



}

// TODO: tell a story with examples

const g = graph```
a -> b -> c
       -> d -> e

c -> e
  -> f -> g
       -> h
  -> i
```;

