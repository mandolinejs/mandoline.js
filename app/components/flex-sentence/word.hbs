{{#each @word.names as |name|}}
  <Rdag::Element
    @id={{name.id}}
  >
    <::Name
      @name={{name}}
    />
  </Rdag::Element>
{{else}}
  <strong>
    <::Name
      @name='?'
    />
  </strong>
{{/each}}
